import { WorkForm } from '../components/Forms/WorkForm';

const FreeNowWork: React.FC = () => {
	return (
		<section>
			<p>Praca we FreeNow!</p>
			<WorkForm />
		</section>
	);
};

export default FreeNowWork;
